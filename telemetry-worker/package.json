{
  "name": "chiefton-telemetry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create chiefton-telemetry",
    "db:init": "wrangler d1 execute chiefton-telemetry --file=./schema.sql",
    "db:init:local": "wrangler d1 execute chiefton-telemetry --local --file=./schema.sql"
  }
}
